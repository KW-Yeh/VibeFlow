// Shared section-eyebrow class — the uppercase, letter-spaced, muted label that
// heads a group of content. Single source of truth (PLAN §2.1 I5); matches the
// sidebar「Projects」heading that set the standard. Compose layout/icon classes
// with `cn(SECTION_LABEL, 'flex items-center gap-2', …)`.
export const SECTION_LABEL =
  'text-xs font-semibold uppercase tracking-wider text-muted-foreground'
