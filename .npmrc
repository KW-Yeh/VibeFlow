; Pin this project to the public npm registry.
; The machine-level ~/.npmrc points at a corporate Artifactory registry for
; other projects; without this override, package-lock.json gets resolved
; against that registry.
registry=https://registry.npmjs.org/
